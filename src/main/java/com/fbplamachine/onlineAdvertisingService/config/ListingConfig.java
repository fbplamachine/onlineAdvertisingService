package com.fbplamachine.onlineAdvertisingService.config;

//@Configuration
public class ListingConfig {
   /* @Bean
    CommandLineRunner commandLineRunner(ListingRepository repository) {
        return args -> {
            Listing rav4 = new Listing(
                    1L,
                    "Rav4",
                    3000.0,
                    State.draft
            );

            Listing starlet = new Listing(
                    1L,
                    "Starlet",
                    2500.0,
                    State.draft
            );

            repository.saveAll(
                    List.of(rav4, starlet)
            );
        };
    }*/
}
